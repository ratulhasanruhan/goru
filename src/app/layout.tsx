import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { LanguageProvider } from '@/contexts/LanguageContext'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'GoruSheba - AI + IoT Cattle Management Platform',
  description: 'Revolutionary cattle management system with IoT devices and artificial intelligence. Transform your farm with smart technology.',
  keywords: 'cattle management, IoT, AI, farming, agriculture, livestock, smart farming',
  authors: [{ name: 'GoruSheba Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <LanguageProvider>
          {children}
        </LanguageProvider>
      </body>
    </html>
  )
}
